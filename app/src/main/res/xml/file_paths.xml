<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Pre-Q exports are written to the public Downloads folder before sharing. -->
    <external-path name="external_downloads" path="Download/" />
    <!-- Fallback exports are written to <cache>/exports when public storage is unavailable. -->
    <cache-path name="cache_exports" path="exports/" />
</paths>
